# neog-camp-dothraki-talk
his app uses a Dothraki translator API
It takes user input text and on click, the api then fetches the value input by the user
and returns the translated value from its database
